const socket = new WebSocket('wss://eliservices.servehttp.com:5010');

socket.addEventListener('open', function (event) { console.log('Connected to server via wss'); });
socket.addEventListener('close', function (event) { console.log('Disconnected from tze!'); });

socket.addEventListener('message', function (event) {
    id = event.data.slice(0,8);
    console.log(event.data)
    if (id == "gdikosbg") { update(JSON.parse(event.data.slice(9))); }
});

function update(data) {
    console.log(data);
}
